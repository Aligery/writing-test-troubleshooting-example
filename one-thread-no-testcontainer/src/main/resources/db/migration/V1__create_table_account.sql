CREATE TABLE account (
                         id BIGSERIAL,
                         account_number int,
                         sum_money_rub bigint,
                         user_id bigint,
                         created_at timestamp
);